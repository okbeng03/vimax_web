# ============================================================
# ViMax Web — 环境变量示例模板
#
# 用法：复制为实际运行用的 .env 文件并填写真实值
#   cp .env.example backend/.env
#
# 后端配置由 backend/src/config.py 读取（pydantic-settings，
# env_prefix="VIMAX_"，env_file 为运行目录下的 .env，即 backend/.env）
# 注意：键必须带 VIMAX_ 前缀才会被读取！
#
# 所有变量均有默认值，仅需按需覆盖；未配置项不影响启动。
# ============================================================

# ── ViMax 执行环境 ──────────────────────────────────────────
# vimax_main 根目录（日志、workflow、ComfyUI view 等资源路径）
VIMAX_VIMAX_ROOT=/Users/wangchangbin/ai/ViMax-main
# 项目工作目录根（各项目 output/ 等子目录的上级）
VIMAX_WORKING_DIR_ROOT=/Users/wangchangbin/ai/vimax_ru

# ── 数据库 ──────────────────────────────────────────────────
# SQLite 连接串（相对 backend 运行目录）
VIMAX_DATABASE_URL=sqlite+aiosqlite:///data/vimax_web.db

# ── ComfyUI ─────────────────────────────────────────────────
# ComfyUI 服务地址（结果预览/回传拉取）
VIMAX_COMFYUI_BASE_URL=http://192.168.3.4:8188

# ── Vimax Scheduler（调度服务）──────────────────────────────
# 调度服务 base URL
VIMAX_SCHEDULE_SERVICE_URL=http://127.0.0.1:8001
# 调度服务鉴权 key（请求头 X-API-Key 携带）；留空则调度操作返回鉴权错误提示
VIMAX_SCHEDULE_API_KEY=

# ── 默认用户 ────────────────────────────────────────────────
VIMAX_DEFAULT_USERNAME=muze

# ── 服务 ────────────────────────────────────────────────────
VIMAX_HOST=0.0.0.0
VIMAX_PORT=8000
VIMAX_DEBUG=true

# ── CORS ────────────────────────────────────────────────────
# JSON 数组字符串，允许的前端来源
VIMAX_CORS_ORIGINS=["http://localhost:5173"]
